# Changelog

## Unreleased

- Isolate state by synchronization job and migrate the legacy schema.
- Reconcile local, remote, and stored snapshots before applying changes.
- Make deletion opt-in with count and percentage safety limits.
- Add plan, dry-run, JSON output, exclusions, and safer credential discovery.
- Add optional provider dependencies and a foundational test suite.
